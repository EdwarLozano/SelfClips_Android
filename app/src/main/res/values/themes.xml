<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MovieFinderApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.MovieFinderApp" parent="Base.Theme.MovieFinderApp" />
    
    <style name="BottomNavigation">
        <item name="android:background">@color/pink</item>
        <item name="itemIconTint">@color/brown</item>
        <item name="itemTextColor">@color/brown</item>
    </style>
</resources>